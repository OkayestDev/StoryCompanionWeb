import React from 'react';
import '../css/NoStorySelected.css';

const NoStorySelected = () => {
    return (
        <div className="noStoryContainer">
            Select a story to begin editing components
        </div>
    );
};

export default NoStorySelected;
